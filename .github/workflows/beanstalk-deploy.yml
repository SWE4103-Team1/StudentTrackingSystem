name: Deploy master
on:
  push:
    branches:
    - feature_cd_dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout source code
      uses: actions/checkout@v2

    - name: Generate deployment package
      run: zip -r deploy.zip . -x '*.git*'

    - name: Deploy to EB
      uses: einaregilsson/beanstalk-deploy@v18
      with:
        aws_access_key: AKIA3HJYUVJP7LCJIR25
        aws_secret_key: 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
        application_name: SWE4103-AWS-BeanstalkServer
        environment_name: Swe4103awsbeanstalkserver-env
        version_label: "v${{ steps.format-time.outputs.replaced }}"
        region: us-east-1
        deployment_package: deploy.zip
